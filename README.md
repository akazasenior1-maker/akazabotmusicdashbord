# ⚛ AKAZA MUSIC BOT | SENIOR V3 ENGINE

هذا هو البوت الجديد كلياً الذي تم بناؤه من الصفر لضمان أعلى مستويات الثبات في ديسكورد والمزامنة الكاملة مع لوحة التحكم.

## 📁 الهيكلية الجديدة (Clean Architecture)
```text
/bot
  ├── bot.py             # العقل المدبر ودمج الخدمات
  ├── music_player.py    # محرك الصوت (yt-dlp + FFmpeg)
  ├── queue_manager.py   # إدارة طوابير السيرفرات
  ├── voice_manager.py   # إدارة الاتصالات الصوتية
  ├── dashboard_bridge.py # جسر المزامنة (API + WebSockets)
  └── config.py          # الإعدادات والتأمين
manager.py              # نقطة الدخول (Entry Point) لـ Render
```

## 🛠️ المميزات الجديدة (Senior Quality)
1. **نظام العملية الموحدة**: البوت ولوحة التحكم يعملان في عملية واحدة لتوفير الذاكرة (RAM).
2. **جودة 192kbps**: صوت نقي جداً بدون تقطيع بفضل خيارات FFmpeg الاحترافية.
3. **مزامنة فورية**: تحديث شريط التقدم والأغاني في الموقع كل 3 ثوانٍ.
4. **ثبات الـ Uplink**: لا وجود لخطأ "Bot Unreachable" بعد الآن.

## 🚀 كيفية التشغيل في Render.com
تأكد من إعداد المتغيرات البيئية التالية:
- `BOT_TOKEN`: توكن البوت.
- `CLIENT_ID`: معرف التطبيق.
- `CLIENT_SECRET`: سر العميل.
- `REDIRECT_URI`: `https://your-app.onrender.com/auth/callback`

**نقطة الدخول (Start Command):**
`python manager.py`

## 🎮 أوامر السلاش (Slash Commands)
- `/play`: تشغيل أغنية برابط أو اسم.
- `/pause`: إيقاف مؤقت.
- `/resume`: استئناف.
- `/skip`: تخطي.
- `/stop`: إيقاف ومسح الطابور.
- `/volume`: التحكم في مستوى الصوت (1-200).

---
تم التطوير بدقة Senior Level لضمان أفضل تجربة مستخدم. 🦾💎🚀
